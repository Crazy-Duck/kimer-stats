doctype html
html
    body
        div
            label(for="from") From:
            input(type="date", id="from")
        div
            label(for="to") To:
            input(type="date", id="to")
        button(id="go") Stats
        script(src='/js/params.js')
